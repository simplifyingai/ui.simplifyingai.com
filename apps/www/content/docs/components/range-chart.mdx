---
title: Range Chart
description: Visualize value ranges, confidence intervals, and bands.
featured: true
component: true
---

<ComponentPreview name="range-chart-demo" description="Range charts display data ranges using various visual encodings, perfect for showing temperature ranges, confidence intervals, or stock price ranges." />

## About

Range Charts display data ranges using various visual encodings. They're perfect for showing temperature ranges, confidence intervals, stock price ranges, or any data with low/high bounds. Supports area bands, bar ranges, and error bar styles.

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.simplifying.ai/r/range-chart.json
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="range-chart" title="components/ui/charts/range-chart.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import { RangeChart } from "@/components/ui/charts"

const data = [
  { x: "Jan", low: -5, high: 8, mid: 2 },
  { x: "Feb", low: -3, high: 10, mid: 4 },
  { x: "Mar", low: 2, high: 15, mid: 8 },
]

export function Example() {
  return (
    <RangeChart
      data={data}
      variant="area"
      showMidLine
    />
  )
}
```

## Examples

### Default

The default range chart displays data ranges as filled areas with optional mid-line showing the median or average value.

<ComponentPreview name="range-chart-demo" />

## Variants

### Error Bars

Scientific-style error bars with caps.

<ComponentPreview name="range-chart-error-bars" />

### Bar Ranges

Vertical bar representation of ranges.

<ComponentPreview name="range-chart-bars" />

## API Reference

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| data | `RangeDataPoint[]` | required | Array of x, low, high, mid data |
| variant | `"area" \| "bars" \| "errorBars"` | `"area"` | Visual style |
| xAxisType | `"linear" \| "time" \| "category"` | `"category"` | X-axis scale type |
| showMidLine | `boolean` | `true` | Show middle value line |
| showPoints | `boolean` | `false` | Show data points |
| fillColor | `string` | `"#3b82f6"` | Range fill color |
| strokeColor | `string` | `"#1e40af"` | Border stroke color |
| midLineColor | `string` | `"#1e40af"` | Mid-line color |
| xAxisLabel | `string` | `undefined` | X-axis label |
| yAxisLabel | `string` | `undefined` | Y-axis label |
| valueFormatter | `function` | `toLocaleString` | Format values |

## Data Types

```tsx showLineNumbers
interface RangeDataPoint {
  x: string | number | Date  // X-axis value
  low: number                // Lower bound of range
  high: number               // Upper bound of range
  mid?: number               // Optional middle value
}
```

## Notes

- Range charts are ideal for visualizing uncertainty, variability, or value boundaries.
- The `area` variant works best for continuous data with many points.
- Use `errorBars` for scientific or statistical data to show confidence intervals.
- The `bars` variant is suitable for categorical data or discrete time periods.
- Ensure low values are always less than high values for proper rendering.
- The optional `mid` value can represent mean, median, or any central tendency measure.
