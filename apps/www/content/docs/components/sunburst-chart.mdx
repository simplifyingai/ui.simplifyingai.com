---
title: Sunburst Chart
description: Radial hierarchical visualization for nested data.
featured: true
component: true
---

<ComponentPreview name="sunburst-chart-demo" description="A sunburst chart displaying hierarchical data as concentric rings" />

## About

The Sunburst Chart displays hierarchical data as concentric rings radiating from the center.

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.simplifying.ai/r/sunburst-chart.json
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="sunburst-chart" title="components/ui/charts/sunburst-chart.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import { SunburstChart } from "@/components/ui/charts"

const data = {
  name: "Total",
  children: [
    {
      name: "Category A",
      children: [
        { name: "A1", value: 50 },
        { name: "A2", value: 30 },
      ],
    },
    { name: "Category B", value: 60 },
  ],
}

export function Example() {
  return <SunburstChart data={data} />
}
```

## API Reference

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| data | SunburstNode | required | Hierarchical data |
| innerRadius | number | 50 | Center hole radius |
| showLabels | boolean | true | Show segment labels |
| padAngle | number | 0.01 | Gap between segments |

## Examples

### Organizational Structure

Company department hierarchy visualization.

<ComponentPreview name="sunburst-chart-organizational-demo" />

### Budget Allocation

Annual budget breakdown by department and category.

<ComponentPreview name="sunburst-chart-budget-demo" />

### File Size Distribution

Project file structure by size.

<ComponentPreview name="sunburst-chart-filesize-demo" />

### Minimal

Simple 2-level hierarchy.

<ComponentPreview name="sunburst-chart-minimal-demo" />

## Notes

- The sunburst chart is ideal for visualizing hierarchical data with multiple nested levels
- Each ring represents a level in the hierarchy, with segments sized proportionally to their values
- Interactive hover states help users explore the data structure
- The `innerRadius` prop controls the size of the center hole, allowing for donut-style visualizations
- Use `padAngle` to add visual separation between segments for improved readability
