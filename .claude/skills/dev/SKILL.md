---
name: dev
description: Start the development server
---

Start the development server:

1. Run `pnpm dev` from the project root
2. The server will start at http://localhost:3000
3. Report when the server is ready
